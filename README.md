# local-llm-fit

**手元のPCで動かしたLLMが、実際の業務にどこまで耐えるかを測るツールです。**

同じ業務タスクを何本も同時に投げて、「答えが合っているか」と「どれだけ待たされるか」を
一緒に記録します。出てくるのは、同時に何本まで投げられるかの表です。

> Measures how much real work a locally-run LLM can actually take: it sends the same
> business task at increasing concurrency and records both answer correctness and
> latency, then tells you the largest concurrency that still meets your bar.
> Tasks are Japanese business documents; inputs are synthesised from a fixed seed,
> so no real data is needed and nothing leaves your machine.

## なぜ作ったか

ローカルLLMの情報は「動いた」「毎秒◯トークン出た」に偏っています。
でも業務で知りたいのは、**何人が同時に使っても仕事になるか**です。

この2つは別物です。1人で使うぶんには速いのに、数人が同時に使った瞬間に
待ち時間が跳ね上がる、ということが普通に起きます。
そして速度を測るツールは答えの中身を見ず、答えの品質を測るツールは同時利用を再現しません。
どちらか片方だけでは「使えるか」が判断できないので、両方を一度に測ります。

## 測った結果

- **やらせた仕事**: 日本語の請求書から、決められた項目のJSONを抜き出す（48件）
- **機材**: Apple M5 Max / メモリ128GB（普通のノートPCです）
- **モデル**: `ornith-1.5-35b-a3b` を LM Studio で実行

| 同時に投げた本数 | 最初の文字が出るまで | 返り終わるまで | 1秒あたりの生成量 | 正答率 | 判定 |
|---:|---:|---:|---:|---:|:--|
| 1 | 0.58秒 | 4.32秒 | 85 トークン | 47/48 (98%) | 基準を満たす |
| 2 | 0.65秒 | 7.18秒 | 91 トークン | 47/48 (98%) | 基準を満たす |
| 4 | 0.70秒 | 12.84秒 | 103 トークン | 46/48 (96%) | 基準を満たす |
| 8 | 12.09秒 | 21.84秒 | 107 トークン | 46/48 (96%) | 応答が遅い |
| 16 | 31.57秒 | 39.95秒 | 111 トークン | 47/48 (98%) | 応答が遅い |
| 32 | 89.42秒 | 99.84秒 | 91 トークン | 46/48 (96%) | 応答が遅い |

**この機材とこのモデルなら、同時4本まで。**

表の見方です。

- **同時に投げた本数**: 何本を同時に処理させたか。登録ユーザー数ではありません。
  人は読んだり考えたりしている時間の方が長いので、同時4本さばければ、実際には数十人が使えます
- **最初の文字が出るまで / 返り終わるまで**: 利用者が実際に待つ時間。48件のうち遅い方から3件目の値（p95）
- **1秒あたりの生成量**: その本数のとき、機材全体が1秒間に作れた文字量
- **正答率**: 48件のうち、抜き出したJSONが正解と完全に一致した件数
- **判定**: 「使えると言える基準」を割ったかどうか。基準は
  [`tasks/invoice-json-ja.yaml`](tasks/invoice-json-ja.yaml) に書いてあり、
  ここでは正答率95%以上・最初の文字まで2秒以内・返り終わるまで15秒以内・エラー0 にしています

### 結果から言えること

**待ち時間が壊れる境目は4本と8本の間にあります。**
4本までは最初の文字が0.7秒で出るのに、8本にすると12秒かかります。
これは推論が遅くなったのではなく、順番待ちが始まったということです。
何人まで置けるかを決めているのはこの境目で、モデルの生成速度ではありません。

**本数を増やしても、機材がこなせる仕事量はほとんど増えません。**
1秒あたりの生成量は 85 → 111 トークン（最大でも31%増）で頭打ちになり、
32本では逆に91トークンまで落ちます。
一方で1件が返り終わるまでの時間は 4.32秒 → 99.84秒 と23倍。
つまり**増えるのは待ち時間だけで、こなせる仕事は増えていません。**
毎秒何トークン出るかだけを見ていると、この頭打ちは見えません。

**答えの正しさは、同時本数を増やしても落ちませんでした。**
どの本数でも96〜98%です。落ちるのはほぼ決まった1件で、
品名「名刺印刷 100枚」から「100枚」を落として `名刺印刷` と読み違えます。
まれに出力そのものが壊れることもあり、同じ行を繰り返してJSONが閉じないまま終わっていました。

**片方だけ測っていたら、どちらの結論も出ません。**
速度だけ見れば「毎秒111トークン出ている」で終わり、
品質だけ見れば「96%取れている」で終わります。
並べて初めて「4本まで」という答えが出ます。

## 使う

必要なものは [uv](https://docs.astral.sh/uv/) と、OpenAI互換のAPIを出すローカル実行環境です
（LM Studio、Ollama、vLLM、llama.cpp server など、だいたい該当します）。

```bash
git clone https://github.com/sys1yagi/local-llm-fit
cd local-llm-fit
uv sync

# どんな請求書を渡して、何を正解とするのかを1件だけ見る（LLMは動きません）
uv run fit --dry-run

# 測る
uv run fit --model <モデルID> --concurrency 1,2,4,8,16,32
```

接続先の初期値は `http://localhost:1234/v1`（LM Studio）です。
Ollama なら `--base-url http://localhost:11434/v1` を付けてください。
モデルIDが分からないときは `curl http://localhost:1234/v1/models` で確認できます。

上の設定だと10分ほどかかります。短く試すなら `--samples 12 --concurrency 1,4` で1分程度です。

結果は `results/` に要約のJSON、`runs/` に全部の応答が残ります（`runs/` はGitに入りません）。

## 自分の業務に合わせる

**基準を変える。** `tasks/invoice-json-ja.yaml` の `slo` を書き換えます。
どこまでの間違いを許せるか、何秒まで待てるかは業務ごとに違います。
**ここを決めるのが本来の仕事で、同梱の数字は仮置きです。**

**仕事の内容を変える。** `tasks/` にYAMLを1枚と、`src/local_llm_fit/generators/` に
入力を作るPythonを1本追加します。生成する側は「先に正解を作って、そこから文面を組み立てる」
書き方にしてください。そうすれば本文と正解が必ず一致し、seedを固定すれば
誰の手元でも同じ入力になります。

**実データは使いません。** 入力はその場で合成します。
社外に出せないデータを扱わずに済み、公開しても問題がなく、
評価用のデータが将来モデルの学習に取り込まれても作り直せます。
配るのはデータではなく、データを作る手順です。

## 結果を持ち寄る

`results/` に出たJSONをそのままプルリクエストで送ってもらえると、
機材・モデル・量子化の組み合わせごとの実測が溜まります。
1台で測れることには限りがありますが、集まれば
「この構成なら何本までさばける」という比較表になります。

## 測っていないこと

- 負荷のかけ方は「同時にN本を保ち、1本終わったら次を投げる」だけです。
  実際の利用者は前の応答を待たずに次を送るので、問い合わせが一気に集中する業務では
  この測り方は待ち時間を実際より短く見積もります
- 1つの仕事・1つのモデル・1台の機材での結果です。モデルの優劣を比べる表ではありません
- 消費電力とメモリ使用量は取っていません
- 入力が数百文字の仕事です。数万字を読ませたときの挙動は分かりません

測定条件の詳細と、揃えていない条件の一覧は [METHOD.md](METHOD.md) にあります。

## 他のツールとの違い

- 速度と待ち時間を細かく測るなら [GuideLLM](https://github.com/vllm-project/guidellm) や
  [llm-optimizer](https://github.com/bentoml/llm-optimizer) の方が作りが厚いです。
  ただし返ってきた答えが合っているかは見ません
- 答えの質だけなら [lm-evaluation-harness](https://github.com/EleutherAI/lm-evaluation-harness)、
  日本語なら [llm-jp-eval](https://github.com/llm-jp/llm-jp-eval) や
  [Nejumiリーダーボード](https://nejumi.ai) があります。
  ただし複数人が同時に使う状況は再現しません
- 両方を1つでやる先行研究に [Bench360](https://github.com/slinusc/bench360) があります。
  こちらはNVIDIAのGPU専用で、Apple Silicon や llama.cpp では動きません

このリポジトリが埋めているのは、**日本語の業務書類**、**普通のPC（Apple Silicon を含む）**、
**自分で決めた基準に照らした判定**の3つです。

## ライセンス

MIT
