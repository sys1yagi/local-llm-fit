# 測定条件

数字を読むときに必要な前提をここに書く。
測り方に異論があるときも、まずこのページを見てほしい。

## 固定しているもの

| 項目 | 値 | 備考 |
|:--|:--|:--|
| 入力 | seed 固定で合成 | 同じ seed なら誰の手元でも同じ12件 |
| 温度 | 0 | `tasks/*.yaml` の `request` で変えられる |
| 最大出力トークン | 700 | 同上 |
| プロンプト | タスクYAMLの1種類のみ | プロンプトを工夫した場合の差は測っていない |
| 採点 | 決定論的 | JSONが読めるか / スキーマを満たすか / 値が正解と一致するか |
| リクエストの並び | 全並列数で同じ12件・同じ順序 | 並列数だけを変えている |

## 測っているもの

**TTFT**（Time To First Token）は、リクエストを送ってから**本文の最初の1文字**が届くまでの時間。
思考トークンを先に出すモデルでは、その分がここに乗る。これは測定の誤りではなく、
利用者が実際に画面の前で待つ時間なので、そのまま測っている。

**応答完了**は、リクエスト送信から最後のチャンクを受け取るまでの時間。

**スループット**は、その並列数での「全出力トークン数 ÷ 全体の実時間」。
1リクエストあたりの生成速度ではなく、機材が単位時間に処理できた総量。

**合格率**は、12件のうち採点を通った件数の割合。
1件でも値が違えばその件は不合格で、部分点はつけない。

**パーセンタイル**は最近傍順位法（補間しない）。12件しかないので、
補間すると実測していない値が表に出てしまう。p95 は実質的に上から1件目の値になる。

## 制御していないもの

ここに挙げたものは測定結果に影響するが、このツールでは固定していない。
結果を比較するときは、これらが揃っているかを確認する必要がある。

- **推論サーバ側の並列設定**。LM Studio や Ollama、vLLM はそれぞれ同時処理数の既定値が違う。
  外から見える待ち時間の変化には、モデルの速度ではなくこの設定が効いていることがある
- **量子化の形式とビット幅**。エンドポイントから取れないので記録していない
- **KVキャッシュの割り当てとコンテキスト長の上限**
- **機材の熱状態**。連続で回すと後半の並列数が不利になる。
  並列数を増やす順に測っているので、この影響は「高並列側が実際より悪く出る」方向に働く
- **他プロセスの負荷**。測定中に他のアプリがGPUやメモリを使っていれば影響する
- **モデルのロード状態**。1回目の呼び出しにモデル読み込みが入ると、その分が乗る。
  測定前に1件投げて温めることを推奨する

同じ条件で回し直すと、スループットは1割ほど動く。
小数第1位の違いは意味を持たないと考えてよい。
一方で合格率は温度0のため回し直しても変わらず、落ちる件も同じだった。

## 負荷のかけ方

closed-loop。同時に N 本を保ち、1本終わったら次を投げる。
実際の業務では、利用者は前の応答を待たずに次々とリクエストを出す（open-loop）ため、
バーストのある業務では、この測り方は待ち時間を実際より短く見積もる。

到着間隔を決めた負荷が必要になったら、`src/local_llm_fit/run.py` を
GuideLLM のような専用ツールに差し替えられるよう、採点とは分けてある。

## この測り方で分からないこと

- 長いコンテキスト（数万トークン）での挙動。今のタスクは入力が数百トークン
- 何時間も回し続けたときの劣化やメモリ漏れ
- 複数モデルを同時に載せたときの取り合い
- 実データでの品質。合成入力で測っているので、実際の帳票の癖は反映されていない
