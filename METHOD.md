# 測定条件

数字を読むときに必要な前提をここに書きます。
測り方に異論があるときも、まずこのページを見てください。

## 揃えているもの

| 項目 | 値 | 補足 |
|:--|:--|:--|
| 入力 | seedを固定して合成 | 同じseedなら誰の手元でも同じ入力になる |
| 温度 | 0 | `tasks/*.yaml` の `request` で変えられる |
| 出力の上限 | タスクごとに指定 | 請求書は700、業務日報は1200 |
| 指示文 | タスクYAMLの1種類だけ | 指示文を工夫した場合との差は測っていない |
| 採点 | 決まった手順のみ | JSONとして読めるか / 項目が揃っているか / 値が正解と一致するか |
| 入力の割り当て | 同時本数の行ごとに別の入力。同じ作り方・同じ分布・同じseed由来 | 下の「行ごとに入力を変えている理由」を参照 |
| 投げる順番 | 行の中では毎回同じ順序 | 行の中で変えているのは同時本数だけ |

## 行ごとに入力を変えている理由

同時1本、2本、4本…と測るとき、行ごとに別の入力を使います。
1つの seed から (件数 × 行数) 件をまとめて作り、先頭から順に割り当てます
（48件6行なら、1行目が1〜48件目、2行目が49〜96件目）。
seed が同じなら全体が再現できますし、1回の測定の中に同じ入力は二度現れません。

以前は全部の行で同じ入力を投げ直していました。
すると推論サーバが前の行で読んだ内容を覚えていて、2行目以降は読み込みを省略します。
入力2万字のタスクで、同じ4本同時が **初見 59.69秒 / 2回目 0.40秒** に分かれました。
150倍です。入力が短いタスクでも同じ向きの誤差が入っていました。

この変更には引き換えがあります。**行ごとに別の入力で正答率を測ることになるので、
行間の正答率の差には入力の個体差が混ざります。**
実測では行ごとに83〜98%の幅が出ました。
同時本数そのものが正答率に効いているのかどうかは、この測り方では切り分けられません。

## 測っている値

**最初の文字が出るまで**は、リクエストを送ってから、**答えの本文の1文字目**が届くまでの時間です。
考えている内容を先に出すモデルでは、その分がここに含まれます。
これは測り方の誤りではなく、利用者が実際に画面の前で待つ時間なので、そのまま測っています。

**返り終わるまで**は、リクエストを送ってから最後の文字を受け取るまでの時間です。

**1秒あたりの生成量**は、その同時本数での「全体で作った文字量 ÷ 全体にかかった時間」です。
1件あたりの生成速度ではなく、機材全体が単位時間にこなせた量を表します。

**正答率**は、その行で投げた件数のうち採点を通った件数の割合です。
1つでも値が違えばその件は不正解で、部分点はつけません。
行ごとに別の入力なので、行をまたいで比べるときは入力の個体差が入ります。

**p95** は補間せず、実際に測れた値をそのまま使います（48件なら遅い方から3件目）。
補間すると、測っていない値が表に出てしまうためです。

件数が少なすぎると正答率が粗くなります。12件だと 11/12 = 91.7% の次が 12/12 = 100% で、
その間の値が存在しません。95%のような基準を使うなら40件以上必要です。

## 揃えていないもの

以下は結果に影響しますが、このツールでは固定していません。
結果どうしを比べるときは、これらが揃っているかを確認する必要があります。

- **前回の測定の残りがサーバに残っていること**。
  1回の測定の中では同じ入力を二度投げませんが、
  **同じ seed で測り直すと、サーバが前回の測定で読んだ入力を覚えていることがあります。**
  その場合、読み込みが省略されて実際より速く出ます。
  測り直すときは、推論サーバを再起動するか `--seed` を変えてください。
  コード側では対処していません
- **実行環境側の同時処理数の設定**。LM Studio、Ollama、vLLM でそれぞれ初期値が違います。
  待ち時間の変化には、モデルの速さではなくこの設定が効いていることがあります
- **量子化の方式とビット数**。APIからは取れないので記録していません
- **キャッシュの割り当てと、扱える文字数の上限**
- **機材の熱の状態**。連続で回すと後の方が不利になります。
  同時本数を増やす順に測っているので、この影響は「本数が多い側が実際より悪く出る」方に働きます
- **他のソフトの負荷**。測定中に他のアプリがGPUやメモリを使っていれば影響します
- **モデルが読み込まれているか**。1回目の呼び出しに読み込み時間が入ると、その分が乗ります。
  測る前に1件投げて温めておくことを勧めます

同じ条件で測り直すと、1秒あたりの生成量は1割ほど動きます。
小数第1位の違いに意味はありません。
正答率は温度0なので、同じ入力に対しては測り直してもほぼ変わりません。

## 負荷のかけ方

同時にN本を保ち、1本終わったら次を投げる方式です。

実際の利用者は前の応答を待たずに次のリクエストを送るため、
問い合わせが一気に集中する業務では、この測り方は待ち時間を実際より短く見積もります。
到着の間隔を決めた測り方が必要になったときに差し替えられるよう、
負荷をかける部分（`src/local_llm_fit/run.py`）は採点と分けてあります。

## この測り方では分からないこと

- 何時間も回し続けたときの劣化やメモリの使い切り
- 複数のモデルを同時に載せたときの取り合い
- 実際の書類での正答率。合成した入力で測っているので、現物の帳票の癖は反映されていません
